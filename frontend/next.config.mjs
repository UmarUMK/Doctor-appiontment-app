/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ibb.co", "img.icons8.com", "media.istockphoto.com"], // Add 'i.ibb.co'
  },
};

export default nextConfig;
